export const constants = {
  HEALTH_CONTROLLER: 'health-check',
  swagger_msg: 'This is token-service documentaion',
  token_CONTROLLER: 'token',
  JWT_SECRET: 'abcdkeyManagementabcd',
};

export const descriptionMsg = {
  
};

export const channels={
  CREATE:'key-created',
  UPDATE:'key-updated',
  DELETE:'key-deleted',
}
